# Netlify Function Example

This function is intended to be deployed as a Netlify Function. Store `SUPABASE_SERVICE_ROLE_KEY` in Netlify Build & Deploy environment variables.

Run locally with netlify-lambda or the Netlify CLI.
